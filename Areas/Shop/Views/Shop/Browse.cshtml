@model DahaimMVC.Areas.Shop.Models.Type
@{
    ViewBag.Title = "Browse";
}

<h2>Browsing Genre: @Model.Name</h2>
<ul>
    @foreach (var item in Model.Products)
    {
        <li>
            @item.Title
        </li>
    }
</ul>